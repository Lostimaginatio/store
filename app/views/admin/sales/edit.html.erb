<% content_for :page_title do %>
  <%= link_to 'Акции', admin_sales_url %> /
  <%= @sale.name.capitalize %>
<% end %>

<%= form_for [:admin, @sale] do |f| %>
  <fieldset>
    <%= render partial: 'form', locals: { f: f } %>
    <%= button 'Сохранить', 'save' %>
  </fieldset>
<% end %>
